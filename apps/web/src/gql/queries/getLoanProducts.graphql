query GetLoanProducts($profileId: Long!) {
  getLoanProducts(profileId: $profileId) {
    recommendedLoanProductId
    profileId
    bankName
    loanProductId
    productName
    minInterestRate
    maxInterestRate
    maxLoanLimitAmount
  }
}
